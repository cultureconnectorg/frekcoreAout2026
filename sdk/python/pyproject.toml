[project]
name = "frekcore-sdk"
version = "0.1.0"
description = "FREKCORE Python SDK — Registry API only (Phase 2, Priority 7). See frekcore_sdk/__init__.py for scope."
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.27",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
