{
  "name": "@frekcore/registry-sdk",
  "version": "0.1.0",
  "description": "FREKCORE TypeScript SDK — Registry API only (Phase 2, Priority 7). See src/registryClient.ts for scope.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "pretest": "tsc -p tsconfig.test.json",
    "test": "node --test dist-test/test/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^22.20.1"
  }
}
