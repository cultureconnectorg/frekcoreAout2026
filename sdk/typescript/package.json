{
  "name": "@frekcore/registry-sdk",
  "version": "0.2.0",
  "description": "FREKCORE TypeScript SDK — Registry, Identity (read), and D1-D5 capability clients. See src/index.ts and docs/architecture/FREKCORE_SDK_CONTRACT_V1.md for scope.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "pretest": "tsc -p tsconfig.test.json",
    "test": "node --test dist-test/test/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^22.20.1"
  }
}
